{
  "sms_home": null,
  "comment": "SMS 固定路径：默认由 resolve_home.py 解析（env SMS_HOME > 缓存目录 > 根目录,统一建 SMS/）。若需覆盖，在此填绝对路径或设置环境变量 SMS_HOME。",
  "scan_roots": [
    "~/.kilocode/skills"
  ],
  "permissions_default": {
    "read": true,
    "write": false,
    "execute": false,
    "network": false
  }
}